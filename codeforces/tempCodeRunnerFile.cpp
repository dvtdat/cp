    // for (int i = 0; i <= s.length() - 3; i += 2)
    // {
    //     num[cnt++] = s[i] - '0';
    // }
    // num[cnt++] = s[s.length() - 1] - '0';
    // sort(num, num + cnt);

    // for (int i = 0; i < cnt - 1; ++i) cout << num[i] << '+';
    // cout << num[cnt - 1];